<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.project.pop_flake.ui.home.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/home_fragment">
        <action
            android:id="@+id/action_homeFragment_to_detailsFragment"
            app:destination="@id/detailsFragment" />
    </fragment>
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.project.pop_flake.ui.settings.SettingsFragment"
        android:label="SettingsFragment"
        tools:layout="@layout/setting_fragment">
        <action
            android:id="@+id/action_settingsFragment_to_complaintsDialog"
            app:destination="@id/complaintsDialog" />
    </fragment>
    <fragment
        android:id="@+id/searchFragment"
        android:name="com.project.pop_flake.ui.search.SearchFragment"
        android:label="SearchFragment"
        tools:layout="@layout/search_fragment">
        <action
            android:id="@+id/action_searchFragment_to_detailsFragment"
            app:destination="@id/detailsFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailsFragment"
        android:name="com.project.pop_flake.ui.home.DetailsFragment"
        android:label="DetailsFragment"
        tools:layout="@layout/details_fragment">
        <argument
            android:name="movie"
            app:argType="com.project.pop_flake.model.MovieDetail" />
    </fragment>
    <dialog
        android:id="@+id/complaintsDialog"
        android:name="com.project.pop_flake.ui.complaints.ComplaintsDialog"
        android:label="ComplaintsDialog"
        tools:layout="@layout/complaints_fragment"/>
</navigation>